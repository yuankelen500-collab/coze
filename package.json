{
  "name": "coze-final-project",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "echo 'Build script running...' && npm install"
  },
  "dependencies": {
    "axios": "^1.6.0",
    "jsonwebtoken": "^9.0.2"
  }
}
